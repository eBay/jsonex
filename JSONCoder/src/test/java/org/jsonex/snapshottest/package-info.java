/**
 * Package is copied from sub-module SnapshotTest. Due to circular dependencies, this module can't add dependency to
 * SnapshotTest, so have to copy the whole package here. Please always keep these two packages in sync.
 */
package org.jsonex.snapshottest;